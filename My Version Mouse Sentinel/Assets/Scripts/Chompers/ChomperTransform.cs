using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChomperTransform : MonoBehaviour {
	/*
	public float x;
	public float y;
	public float moveSpeed;
	public float moveMostSpeed;
	public float moveLeastSpeed;
	//for speed of Chompers

	Vector2 sizeValue;
	public float minimumSize;
	public float maximumSize;
	public float speedSize;
	//for size of Chompers

	void Start () 

	{
		speedSize = Time.deltaTime;
	}

	void Update () 
	{
		x = Random.Range(moveLeastSpeed, moveMostSpeed);
		y = Random.Range(moveLeastSpeed, moveMostSpeed);
		transform.Translate (x, y, 0);
		sets speed of chomper

		transform.localScale = minimumSize;
		if (transform.localScale = minimumSize)
			{
			//*********working part**********
				sizeValue = transform.localScale;
				sizeValue.x += speedSize;
				sizeValue.y += speedSize;
				transform.localScale = sizeValue;
			//*********working part**********
			}
				
		if (transform.localScale = maximumSize)
			{
				sizeValue = transform.localScale;
				sizeValue.x -= speedSize;
				sizeValue.y -= speedSize;
				transform.localScale = sizeValue;
			}
		//increases size of chomper

	}
	*/
}
